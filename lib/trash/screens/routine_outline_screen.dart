import 'package:flutter/material.dart';

class RoutineOutlineScreen extends StatelessWidget {
  const RoutineOutlineScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Routine Outline Screen"),
      ),
    );
  }
}
